"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";

import { appointmentStatusClass, appointmentStatusLabel, formatAppointmentTime } from "@/components/medical-appointments";
import { MedicalDocumentUploader } from "@/components/medical-document-uploader";
import type { PatientRecord } from "@/components/medical-patient-record";
import { apiJson, type MedicalAppointment } from "@/lib/api/client";

type Specialty = { id: string; name: string };

export function MedicalReviewWorkflow({ appointmentId }: { appointmentId: string }) {
  const router = useRouter();
  const [appointment, setAppointment] = useState<MedicalAppointment | null>(null); const [record, setRecord] = useState<PatientRecord | null>(null); const [encounterId, setEncounterId] = useState<string | null>(null);
  const [stage, setStage] = useState<"ready" | "active" | "form">("ready"); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [specialties, setSpecialties] = useState<Specialty[]>([]);
  const [chiefComplaint, setChiefComplaint] = useState(""); const [assessment, setAssessment] = useState(""); const [diagnosisText, setDiagnosisText] = useState(""); const [instructions, setInstructions] = useState(""); const [followUpText, setFollowUpText] = useState(""); const [bloodChemistryStatus, setBloodChemistryStatus] = useState("NOT_PRESENTED"); const [refer, setRefer] = useState(false); const [referralSpecialtyId, setReferralSpecialtyId] = useState(""); const [referralReason, setReferralReason] = useState(""); const [referralComment, setReferralComment] = useState(""); const [referralPriority, setReferralPriority] = useState("ROUTINE");

  useEffect(() => { let active = true; (async () => { try { const appointments = await apiJson<MedicalAppointment[]>("/api/medical/appointments"); const current = appointments.find((item) => item.id === appointmentId); if (!current?.patient) throw new Error("No se encontró una cita asignada a tu cuenta."); const patientRecord = await apiJson<PatientRecord>(`/api/medical/patients/${current.patient.id}`); if (!active) return; setAppointment(current); setRecord(patientRecord); const existing = patientRecord.encounters.find((item) => item.appointment_id === appointmentId); if (existing?.status === "DRAFT") { setEncounterId(existing.id); setStage("active"); setChiefComplaint(existing.chief_complaint === "Pendiente de entrevista clínica." ? "" : existing.chief_complaint); } } catch (reason) { if (active) setError(reason instanceof Error ? reason.message : "No se pudo abrir la cita."); } finally { if (active) setLoading(false); } })(); return () => { active = false; }; }, [appointmentId]);

  async function start() { setSaving(true); setError(""); try { const data = await apiJson<{ encounterId: string }>(`/api/medical/appointments/${appointmentId}/start`, { method: "POST" }); setEncounterId(data.encounterId); setStage("active"); } catch (reason) { setError(reason instanceof Error ? reason.message : "No se pudo iniciar la cita."); } finally { setSaving(false); } }
  async function showForm() { setStage("form"); if (!specialties.length) try { setSpecialties(await apiJson<Specialty[]>("/api/specialties")); } catch (reason) { setError(reason instanceof Error ? reason.message : "No se pudieron cargar especialidades."); } }
  async function finalize(event: React.FormEvent) { event.preventDefault(); if (!encounterId) return; setSaving(true); setError(""); try { await apiJson(`/api/medical/encounters/${encounterId}/finalize`, { method: "POST", body: JSON.stringify({ chiefComplaint, assessment, diagnosisText, instructions, followUpText, bloodChemistryStatus, referralSpecialtyId: refer ? referralSpecialtyId : null, referralReason: refer ? referralReason : null, referralComment: refer ? referralComment : null, referralPriority }) }); router.push(`/medico/pacientes/${record!.patient.id}?actualizada=1`); router.refresh(); } catch (reason) { setError(reason instanceof Error ? reason.message : "No se pudo finalizar la atención."); } finally { setSaving(false); } }

  if (loading) return <p className="py-16 text-center text-sm text-text-secondary">Cargando cita y ficha clínica…</p>;
  if (!appointment || !record) return <p className="rounded-xl bg-error-container p-5 text-error">{error || "No se encontró la cita."}</p>;
  const closed = appointment.status === "ATTENDED" || record.encounters.some((item) => item.appointment_id === appointmentId && item.status === "CLOSED");
  return <><Link href={`/medico/pacientes/${record.patient.id}`} className="text-sm font-semibold text-primary hover:underline">← Ficha del paciente</Link><header className="mt-5 rounded-2xl border border-divider bg-surface p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold tracking-wide text-primary">CITA DE REVISIÓN</p><h1 className="mt-1 text-3xl font-bold text-text-primary">{record.patient.fullName}</h1><p className="mt-2 text-text-secondary">{formatAppointmentTime(appointment.scheduledFor)} · CI {record.patient.carnet}</p></div><span className={`rounded-full px-3 py-1.5 text-sm font-semibold ${appointmentStatusClass(closed ? "ATTENDED" : appointment.status)}`}>{appointmentStatusLabel(closed ? "ATTENDED" : appointment.status)}</span></div><div className="mt-6 grid gap-4 border-t border-divider pt-5 text-sm md:grid-cols-3"><p><span className="block text-text-secondary">Cuestionario inicial</span><span className="font-semibold text-text-primary">{record.intake?.allergies ? `Alergias: ${record.intake.allergies}` : "Sin alergias declaradas"}</span></p><p><span className="block text-text-secondary">Atenciones previas</span><span className="font-semibold text-text-primary">{record.encounters.filter((item) => item.status === "CLOSED").length} registrada(s)</span></p><p><span className="block text-text-secondary">Archivos disponibles</span><span className="font-semibold text-text-primary">{record.documents.length} documento(s)</span></p></div></header>{error && <p className="mt-5 rounded-lg bg-error-container p-4 text-sm text-error">{error}</p>}{closed ? <section className="mt-7 rounded-2xl bg-success-container p-6"><h2 className="font-bold text-success">Esta cita ya fue atendida</h2><p className="mt-2 text-sm text-text-secondary">La evolución está en la ficha. La derivación, si se creó, espera la asignación administrativa a un especialista.</p></section> : stage === "ready" ? <section className="mt-7 rounded-2xl border border-primary-200 bg-primary-container p-6"><h2 className="text-xl font-bold text-text-primary">1. Iniciar cita</h2><p className="mt-2 max-w-2xl text-sm text-text-secondary">Se abrirá un borrador clínico para esta cita; aún no cerrará ni modificará el historial.</p><button onClick={start} disabled={saving} className="mt-5 rounded-lg bg-primary px-5 py-3 text-sm font-bold text-on-primary disabled:opacity-50">{saving ? "Iniciando…" : "Iniciar cita"}</button></section> : stage === "active" ? <section className="mt-7 rounded-2xl border border-warning-container bg-warning-container p-6"><h2 className="text-xl font-bold text-text-primary">2. Cita en curso</h2><p className="mt-2 text-sm text-text-secondary">Entrevista al paciente y revisa su ficha. Cuando termines, completa la evolución y adjunta sus exámenes.</p><button onClick={showForm} className="mt-5 rounded-lg bg-primary px-5 py-3 text-sm font-bold text-on-primary">Terminar cita y registrar atención</button></section> : <form onSubmit={finalize} className="mt-7 space-y-6"><section className="rounded-2xl border border-divider bg-surface p-5 sm:p-6"><p className="text-sm font-semibold tracking-wide text-primary">3. EVOLUCIÓN DE REVISIÓN</p><h2 className="mt-1 text-xl font-bold text-text-primary">Registro de atención</h2><p className="mt-1 text-sm text-text-secondary">Al cerrar, la evolución será parte del historial clínico longitudinal.</p><div className="mt-6 grid gap-5"><label><span className="text-sm font-semibold text-text-primary">Motivo de consulta *</span><textarea required value={chiefComplaint} onChange={(event) => setChiefComplaint(event.target.value)} rows={3} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label><label><span className="text-sm font-semibold text-text-primary">Evaluación clínica *</span><textarea required value={assessment} onChange={(event) => setAssessment(event.target.value)} rows={5} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label><div className="grid gap-5 md:grid-cols-2"><label><span className="text-sm font-semibold text-text-primary">Diagnóstico principal</span><input value={diagnosisText} onChange={(event) => setDiagnosisText(event.target.value)} placeholder="Diagnóstico o impresión clínica" className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label><label><span className="text-sm font-semibold text-text-primary">Química sanguínea</span><select value={bloodChemistryStatus} onChange={(event) => setBloodChemistryStatus(event.target.value)} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary"><option value="NOT_PRESENTED">No presentada</option><option value="PENDING">Pendiente</option><option value="ATTACHED">Adjunta</option></select></label></div><MedicalDocumentUploader patientId={record.patient.id} encounterId={encounterId!} fixedType="BLOOD_CHEMISTRY" compact onUploaded={() => setBloodChemistryStatus("ATTACHED")} /><label><span className="text-sm font-semibold text-text-primary">Indicaciones</span><textarea value={instructions} onChange={(event) => setInstructions(event.target.value)} rows={3} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label><label><span className="text-sm font-semibold text-text-primary">Seguimiento sugerido</span><input value={followUpText} onChange={(event) => setFollowUpText(event.target.value)} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label><MedicalDocumentUploader patientId={record.patient.id} encounterId={encounterId!} /></div></section><section className="rounded-2xl border border-divider bg-surface p-5 sm:p-6"><label className="flex cursor-pointer items-start gap-3"><input type="checkbox" checked={refer} onChange={(event) => setRefer(event.target.checked)} className="mt-1 size-4 accent-primary" /><span><span className="font-bold text-text-primary">Derivar a una especialidad</span><span className="mt-1 block text-sm text-text-secondary">Administración asignará el caso al especialista y a su cupo disponible.</span></span></label>{refer && <div className="mt-6 grid gap-5 border-t border-divider pt-5"><div className="grid gap-5 md:grid-cols-2"><label><span className="text-sm font-semibold text-text-primary">Especialidad *</span><select required value={referralSpecialtyId} onChange={(event) => setReferralSpecialtyId(event.target.value)} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary"><option value="">Seleccionar…</option>{specialties.map((specialty) => <option key={specialty.id} value={specialty.id}>{specialty.name}</option>)}</select></label><label><span className="text-sm font-semibold text-text-primary">Prioridad *</span><select value={referralPriority} onChange={(event) => setReferralPriority(event.target.value)} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary"><option value="ROUTINE">Rutinaria</option><option value="PRIORITY">Prioritaria</option><option value="URGENT">Urgente</option></select></label></div><label><span className="text-sm font-semibold text-text-primary">Motivo de derivación *</span><textarea required value={referralReason} onChange={(event) => setReferralReason(event.target.value)} rows={3} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label><label><span className="text-sm font-semibold text-text-primary">Resumen para el especialista *</span><textarea required value={referralComment} onChange={(event) => setReferralComment(event.target.value)} rows={4} className="mt-2 w-full rounded-lg border border-divider bg-surface px-3 py-2.5 outline-none focus:border-primary" /></label></div>}</section><div className="flex flex-wrap justify-end gap-3"><Link href={`/medico/pacientes/${record.patient.id}`} className="rounded-lg border border-divider px-5 py-3 text-sm font-semibold text-text-primary hover:bg-surface-secondary">Cancelar</Link><button disabled={saving} className="rounded-lg bg-primary px-5 py-3 text-sm font-bold text-on-primary disabled:opacity-50">{saving ? "Guardando…" : refer ? "Cerrar atención y enviar derivación" : "Cerrar atención"}</button></div></form>}</>;
}
