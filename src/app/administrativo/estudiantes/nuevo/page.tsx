"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";

import { AppHeader } from "@/components/app-header";
import { AppSidebar } from "@/components/app-sidebar";
import { apiJson, type CareerOption } from "@/lib/api/client";

type FormState = { fullName: string; carnet: string; registrationCode: string; birthDate: string; careerId: string; academicPeriod: string; enrollmentStart: string; email: string; phone: string };
const initialForm: FormState = { fullName: "", carnet: "", registrationCode: "", birthDate: "", careerId: "", academicPeriod: "2026-II", enrollmentStart: new Date().toISOString().slice(0, 10), email: "", phone: "" };

export default function NewStudentPage() {
  const router = useRouter();
  const [form, setForm] = useState<FormState>(initialForm);
  const [careers, setCareers] = useState<CareerOption[]>([]);
  const [error, setError] = useState("");
  const [saving, setSaving] = useState(false);
  useEffect(() => { apiJson<CareerOption[]>("/api/careers").then(setCareers).catch((cause) => setError(cause instanceof Error ? cause.message : "No se pudieron cargar las carreras.")); }, []);
  function update<K extends keyof FormState>(key: K, value: FormState[K]) { setForm((current) => ({ ...current, [key]: value })); setError(""); }
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); try { const result = await apiJson<{ id: string }>("/api/patients", { method: "POST", body: JSON.stringify(form) }); router.push(`/administrativo/estudiantes/${result.id}`); } catch (cause) { setError(cause instanceof Error ? cause.message : "No se pudo guardar el registro."); setSaving(false); } }
  return <div className="min-h-screen bg-background md:flex"><AppSidebar role="administrativo" active="students" /><main className="min-w-0 flex-1 pb-22 md:pb-0"><AppHeader role="administrativo" clinicalRole="ADMINISTRATIVE" name="Administración" /><div className="mx-auto max-w-3xl p-5 sm:p-8"><Link href="/administrativo/estudiantes" className="text-sm font-semibold text-primary hover:underline">← Volver a estudiantes</Link><header className="mt-5"><p className="text-sm font-semibold tracking-wide text-primary">ADMISIÓN ADMINISTRATIVA</p><h1 className="mt-1 text-3xl font-bold text-text-primary">Registrar estudiante</h1><p className="mt-2 text-text-secondary">Al guardar se crea el paciente, su historia clínica vacía y la matrícula académica activa.</p></header>{error && <p role="alert" className="mt-6 rounded-xl bg-error-container p-4 text-sm text-error">{error}</p>}<form onSubmit={submit} className="mt-7 rounded-2xl border border-divider bg-surface p-5 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><label className="sm:col-span-2"><span className="text-sm font-semibold text-text-primary">Nombre completo</span><input required value={form.fullName} onChange={(event) => update("fullName", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Número de carnet</span><input required value={form.carnet} onChange={(event) => update("carnet", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Código de registro</span><input required value={form.registrationCode} onChange={(event) => update("registrationCode", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Fecha de nacimiento</span><input required type="date" value={form.birthDate} onChange={(event) => update("birthDate", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Carrera</span><select required value={form.careerId} onChange={(event) => update("careerId", event.target.value)} className="mt-2 w-full rounded-lg border border-border bg-surface px-3 py-2.5"><option value="">Selecciona una carrera</option>{careers.map((career) => <option key={career.id} value={career.id}>{career.name}</option>)}</select></label><label><span className="text-sm font-semibold text-text-primary">Período académico</span><input required value={form.academicPeriod} onChange={(event) => update("academicPeriod", event.target.value)} placeholder="2026-II" className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Inicio de matrícula</span><input required type="date" value={form.enrollmentStart} onChange={(event) => update("enrollmentStart", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Correo electrónico</span><input type="email" value={form.email} onChange={(event) => update("email", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label><label><span className="text-sm font-semibold text-text-primary">Teléfono</span><input value={form.phone} onChange={(event) => update("phone", event.target.value)} className="mt-2 w-full rounded-lg border border-border px-3 py-2.5" /></label></div><div className="mt-7 flex justify-end gap-3"><Link href="/administrativo/estudiantes" className="rounded-lg border border-divider px-4 py-2.5 text-sm font-semibold text-text-secondary">Cancelar</Link><button disabled={saving || !careers.length} className="rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-on-primary disabled:opacity-60">{saving ? "Guardando…" : "Guardar registro administrativo"}</button></div></form></div></main></div>;
}
